spring:
  datasource:
    driverClassName: org.postgresql.Driver  # JDBC ??????? ?? PostgreSQL
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/docks?ssl=true&sslmode=require
    username: ${DB_USER:postgres}  # ????????????? ??? ?? ?????? ?????
    password: ${DB_PASSWORD:12345}  # ?????? ?? ?????? ?????

  profiles:
    active: dev

  jpa:
    hibernate:
      ddl-auto: update  # ??????????? ????????????? ?? ???????
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect  # Hibernate ??????? ?? PostgreSQL
        format_sql: true  # ?????? ?????? ?? SQL ????????
    defer-datasource-initialization: true  # ????????? ?? ????????????? ?? ????????? ?? ?????

  sql:
    init:
      mode: never  # ?? ????????? ??????????? SQL ????????? (???? ?? ?? ???????? ?? `always`, ??? ??? ?????)

logging:
  level:
    org.hibernate.SQL: DEBUG  # ??????? ?? ????????????? SQL ??????
    org.hibernate.orm.jdbc.bind: TRACE  # ??????? ?? ??????????? ?? SQL ??????
#    com.vaadin: DEBUG  # ??????? ?? ?????? ????????? ?? Vaadin





